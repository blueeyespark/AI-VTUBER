# BlueIdentity

BlueIdentity stores the shared Blue ID, creator/steward records, trusted devices, Constitution text, and identity metadata.

BlueIdentity is not per-PC. Each PC may have its own BlueNode, but every node points back to the same Blue identity record.

Sensitive identity and Constitution changes must be approval-gated and logged to BlueLedger.
